
npm (gerenciador de pacotes)
|___ package.json (principal arquivo configuração)

npm init (inicia um novo package.json)
npm ls (lista os pacotes instalados)
npm i ou npm install (instalar um novo pacote)

________________________________________________

cd pasta [enter] vai para o(diretório)
cd .. [enter] volta um nível

Estruturas de controle
Condicionais

if (condição) {
    //condição verdadeira

} 

if (condição){
    condição verdadeira
}else {
    condição falsa
}

Observação: Uma condição nunca pode ser verdadeira e falsa ao mesmo tempo