1- Correção de exercicios

- calcular a quantidade de tijolos por metros quadrado (tijolo baiano por metro quadrado)
 (tijolo baiano 8 furos)

 tijolo = 0,0361 m2
 
 //variáveis
 m2, total, (numéricas)

 // entrada
 m2

 //processamentos
 total = m2/0,0361

 //saida
 total

2) calcular a área e o perímetros de um retângulo

Área = base x altura
Perímetro = 2 (base x altura)

//variáveis
area, base, altura, Perímetro (numéricas)

//entrada
 base, altura

//processamento 1
 area = base * altura
 //processamento 2
 Perímetro = base + altura + base + altura 

//saida 1
area
//saida 2
Perímetro
 
3) 
____________________________________________________

Estrutura de Controle Condicional
Encadeamento/aninhamento if - else

if (condição 1) {
    //condição 1 verdadeira
} else {
    if (condição 2 ) {
        //condição 2 verdadeira
} else {
    //condição 2 falsa
    }
}

if (condição 1) {
    //condição 1 verdadeira
} else {
    if else (condição 2 ) {
    //condição 2 verdadeira
} else {
    //nenhuma condição anterior verdadeira
}

exercicios

Desenvolver no scratch e no node.js um app que informe a obrigatoriedade de voltar
de votar em função da idade, sabendo que:
 - abaixo de 16 anos o voto é proibido
 - 16,17 ou acima de 70 anos o voto é facultativo
 - entre 18 e 70 anos (incluindo o 18 e 70 ) o voto é obrigatório

//variáveis
idade
//processamento e saida
Encadeamento if - else